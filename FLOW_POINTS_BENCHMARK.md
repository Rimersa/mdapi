# flow_points 0.6.0 实测（2026-09-21）

以下为 0.6.0 上线前进行的隔离性能测试：仅在 85 本机进行，当时使用候选构建、未部署或重启 87 正式服务。数值保留原始测试条件，不能当作跨机器耗时。

## 条件与数据

- 本机 localhost 网关与客户端分为独立进程，客户端设置 cores=2；每个用例启动全新的客户端进程，网关使用系统 Python 标准库。
- 原始日文件：2024-02-02，62,890,763 行、747,568,876 字节；2026-09-02，80,267,316 行、973,220,328 字节。
- 样本库仅挂载这两个完整日文件，跨日用例代表读取这两个文件，不是中间所有历史日期。
- 未清理系统页缓存；网关可复用 footer 元数据缓存。这是本机现有缓存条件下的结果，不是冷盘或跨机器网络速度承诺。
- 计时包括创建客户端、接收并消费全部 Arrow 批次、计数及 volume 序列 SHA-256 核对；首批延迟也包含首次客户端/Arrow 初始化。

## 结果

| 查询 | 返回行数 | 总耗时 | 首批延迟 | 业务字节传输 | 客户端峰值 RSS | 网关峰值 RSS |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 单日全市场，10列 | 80,267,316 | 20.58s | 0.756s | 927.32 MiB | 360.6 MiB | 29.9 MiB |
| 单日 000001.SZ，10列 | 49,678 | 1.85s | 0.717s | 73.44 MiB | 188.4 MiB | 29.9 MiB |
| 同一只股票，强制顺序扫描对照 | 49,678 | 13.56s | 0.517s | 927.32 MiB | 310.9 MiB | 29.9 MiB |
| 单日10只股票，4列 | 444,141 | 4.10s | 0.693s | 263.96 MiB | 211.6 MiB | 29.5 MiB |
| 两日全市场，10列 | 143,158,079 | 27.16s | 0.658s | 1639.76 MiB | 342.2 MiB | 30.5 MiB |

单股查询相对整日压缩文件少传约 92.09%，相对该单股强制顺序扫描对照耗时降低约 86.35%。十股票四列少传约 71.56%。行组包含多只股票，筛选结果少不代表传输字节能同比缩小；本版不重写现有存储布局。

两日返回量增加到 143,158,079 行时，客户端峰值仍约 342 MiB，说明此用例中内存没有随总行数线性增长。峰值以 10ms 间隔采样，包含解释器、Arrow 和库开销；单个特别大的行组或调用者主动保留批次仍可能提高内存。

## 正确性与回归

- 每个真实用例与独立直接 Parquet 读取的返回行数、volume 完整行序列 SHA-256 一致。测试文件的 inode、大小和修改时间保持不变，隔离网关均已退出。
- 小规模端到端测试逐列比较原表与 API 结果，覆盖日期首尾、上海/UTC 时间、每日窗口、投影辅助列不泄漏、空股票/空文件、partial、缺日、版本切换、源修改、重复日期和链接越界。
- 认证、两用户并发、片段中途断开重试、主动取消、HTTP 中途失败不得伪装成完整表，以及原三类逐笔和缓存功能纳入回归。
- 原始测量明细：[benchmarks/points_20260921.json](benchmarks/points_20260921.json)。完整用法：[FLOW_POINTS.md](FLOW_POINTS.md)。
