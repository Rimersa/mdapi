# Market Data API 0.5.0

现在可以用 `symbols`、`columns` 和时间窗口共同决定读取内容。少数股票查询会利用现有 Parquet 行组统计只传相关数据块；全市场读取保留顺序通道。**无需改动现有 Parquet 或 catalog v2。**

## 新功能

- 股票精确过滤，包含股票列未被选择时的内部过滤。
- 行组/列块裁剪、相邻读取合并和可配置的机械盘/SSD 成本预设。
- 标准 HTTP Range 与批量字节段传输。
- 只读源文件的压缩元数据缓存；持久缓存放在行情目录之外。
- `MarketDataClient.connect()` 原生客户端，直接复用网关连接并返回 Arrow 批次。
- 原生安装方式 `install-client.sh --native` 和可选元数据预热工具。
- 传输字节、读取路径、跳过对象、重试和排队统计。

## 正确性与并发

- 未知参数明确拒绝；不存在的股票返回有 schema 的空结果。
- SDK 检查本机 API 是否确认股票过滤，避免新参数被旧服务忽略。
- 网络中断按未完成对象恢复，不重复已完成结果。
- 修复 direct/cache 资源获取顺序相反造成的等待风险。
- 同时缺失的本地缓存请求合并填充，固定读取所用的文件列表。
- current 指向缺失版本索引时明确报错。
- 首块失败在 HTTP 成功响应之前报告；流中途失败关闭连接以便客户端识别截断。
- 元数据、网络和解码按有限窗口处理；取消读取释放连接和内存额度。

## 升级

服务器和客户端均升级至 0.5 可获得按需传输。0.4 的完整对象客户端仍可连接新版网关。

安装器保留已有用户、令牌和服务地址/并发设置。现有数据格式不变，新增的只有服务自身的可重建元数据缓存。用户服务沿用原部署模式，不需要把它改成系统服务。

测试范围、实测数字和限制见 BENCHMARK.md。完整结果在用户程序保留多大内存仍由用户决定，`iter_batches` 不会替用户释放其保存的批次。
