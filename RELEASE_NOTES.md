# Market Data API 0.4.1

这是当前正式交付版。服务器端是标准库单文件只读网关；用户端是本机 FastAPI
与 Python SDK，同时支持不落地的 `direct` 和增量本地缓存的 `cache`。

## 生产边界

- 上游程序负责产生符合 `SERVER_DATA_FORMAT.md` 的5分钟 Parquet 成品目录和 catalog；
- 网关只读成品目录，不读原始湖、不切片、不转换、不修改数据；
- 每日新数据由上游原子发布后，网关自动热读取，无需重启。

## 主要能力

- 固定大小根索引和按数据集/交易日/版本分片的 catalog v2；
- 对象级断线恢复，已完成对象不重拉；
- 最多两条远端流按用户公平轮转，单用户可借用空闲流；
- 本机依 CPU affinity 和可用内存自适应，也支持 `--cores 1`；
- 零用户默认锁定，用户令牌可热新增、轮换和移除。

## 0.4.1 文档修正

- 移除了面向用户的五天测试集制作命令和公开 `mdapi-build` 入口；它们不是部署或生产每日任务；
- 补充五天样本的精确可还原性说明：所有输入行、列和值均可完整还原；
  不保证还原 Parquet 的原字节、文件分块、row-group 和物理行顺序。

0.4.1 与 0.4.0 的用户 API 兼容，并仍可读取旧单文件 catalog 以便迁移。
