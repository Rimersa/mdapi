# Market Data API 0.4.0

这是首个正式交付版本。用户侧保持`mdapi-local + FastAPI + Python SDK`流程不变，服务器
侧使用只读标准库单文件网关。

主要能力：

- 根catalog固定约400字节，对象描述按数据集、交易日和不可变版本分片；
- 网关按请求日期懒加载，64个指针和64个版本分片的有界LRU避免历史增长拖慢启动；
- 每日新数据和历史修订可原子热发布，旧请求继续读取其已取得的不可变版本；
- direct与cache返回相同Arrow Stream，cache按五分钟桶增量填充；
- 默认对象级断线恢复，完成对象不重拉，cache保留已经提交的完整桶；
- 两条远端流按用户公平轮转，单用户可借用空闲流；
- 本机依据CPU affinity和可用内存自适应，也支持`--cores 1`；
- 零用户部署默认锁定，用户令牌可以热新增、轮换和移除。

兼容性说明：0.4.0仍可读取旧单文件catalog以便迁移，但正式部署只使用catalog v2。
迁移默认不修改任何Parquet，也不留下旧格式副本；显式`--keep-backup`才保留额外备份。
