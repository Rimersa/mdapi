# 五天 5 分钟派生层与 API 基准

测试时间：2026-07-31。客户端为 `10.10.10.85`，87 为 `10.10.10.87`。

两端虚拟网卡都显示 1Gbps，但同一虚拟交换网络上的实际内存热数据吞吐明显超过
1Gbps。因此结果同时给出真实 MB/s；不能把虚拟网卡显示值当成实际限速器。

## 1. 五天测试数据

日期：

```text
2026-05-25
2026-05-26
2026-05-27
2026-05-28
2026-05-29
```

原始源数据只读，共 61,176,191,221 bytes。派生目录为：

```text
/home/quant/market_data_api_5m_example_v2
```

派生结果：

| dataset | rows | objects | bytes |
|---|---:|---:|---:|
| orders | 2,277,092,658 | 1,286 | 24,500,623,734 |
| trades | 1,266,332,561 | 758 | 17,104,346,166 |
| snapshots | 147,103,975 | 365 | 11,306,973,618 |
| 合计 | 3,690,529,194 | 2,409 | 52,911,943,518 |

这里的 object 是5分钟逻辑桶内的物理 Parquet part。一次 API 请求会在同一持久HTTP
连接上按小bundle分段传输，不会逐文件重新建连接；默认每段最多12个对象，用于公平轮转
和对象级自动续传。

15个分区全部执行：

- 源/输出总行数一致性校验；
- schema 一致性校验；
- size/mtime 源指纹；
- 独立暂存后原子发布。

### 数据内容的完整可还原性

当前五天样本可以完整还原为生成该样本时的输入数据内容。切片过程没有过滤、
去重、增加业务行、删除业务列或改写单元格值。对15个 dataset-day 分区的校验结果是：

- 每个分区的输入行数与全部5分钟对象的行数之和完全相等；
- 列名和列顺序完全相同；
- 用于分区的临时桶键不写入成品；
- 唯一的 Arrow 类型表示差异是文本列从 `string` 扩为 `large_string`，字符串值不变，
  需要时可无损 cast 回 `string`。

还原时只需读取指定 dataset、trade_date 和 version 的所有对象，再做纵向合并；
得到的是原输入的全部行、列和值。本项目不保留原 Parquet 的字节序列、文件边界、
row-group 布局、压缩结果和行的物理存储顺序；这些是存储布局，不是数据内容。

开发期间重新运行制作样本的内部程序时，15个分区均识别为
`already_published`，总墙钟时间18.42秒，没有重新生成数据。这是测试集制作记录，
不是网关的部署步骤或生产每日任务。

源安全审计：

```json
{"manifests":15,"source_files_checked":200,"mismatches":[]}
```

### catalog v2元数据性能

用上述真实2409对象目录比较旧单文件索引和最终分片索引：

| 请求范围 | 格式 | 初始化 | 首次选择 | Python峰值内存 |
|---|---|---:|---:|---:|
| 1日 | 单文件 | 64.3ms | 6.0ms | 3.979MiB |
| 1日 | 分片v2 | 1.49ms | 2.77ms | 0.126MiB |
| 5日 | 单文件 | 39.7ms | 4.28ms | 3.975MiB |
| 5日 | 分片v2 | 1.33ms | 12.1ms | 0.394MiB |

同一进程热选择时，单日由约1012微秒降到191微秒，五日约923微秒。五日v2首次选择
需要分别打开15个小分片，因此比已经整表载入内存的旧格式多约8毫秒；这是一次性且只与
请求天数相关的成本。根索引保持400字节，正式历史继续增长不会增加启动读取量，LRU默认
最多保留64个当前指针和64个版本分片。

## 2. 测试集制作速度（不属于网关）

4核阶段受共享机械卷负载影响较大，代表分区耗时从95秒到640秒。调整为单分区8核后：

| dataset/date | 4核同类参考 | 8核实测 |
|---|---:|---:|
| trades 2026-05-28 | 260–524s | 119.75s |
| snapshots 2026-05-28 | 100–527s | 84.31s |
| orders 2026-05-29 | 146–640s | 119.78s |
| trades 2026-05-29 | 260–524s | 49.45s |
| snapshots 2026-05-29 | 100–527s | 86.74s |

8核仍只有一个机械盘源读取流，没有并行构建多个 dataset-day，给12核服务器保留4核。

## 3. 87 零拷贝网关

网关使用：

- 常驻 HTTP/1.1；
- 两个建立后长期复用的连接；
- 已压缩 Parquet 原样传输；
- 持久连接上的小bundle分段；
- Linux `sendfile`；
- Linux 页缓存；
- 不启动 SSH、Shell、DuckDB、PyArrow 或 FastAPI 子进程。

最终v2网关在87零用户启动后的RSS实测为26904KiB（约26.3MiB），此时根索引已加载，
当前指针和版本分片缓存均为0。请求只会按日期填充有界LRU，不再把2409个对象的全部
描述常驻内存。数据传输路径未改变，既有满速测试中进程峰值约29MB、平均约占
0.2–0.5个核心；空闲CPU接近0。Linux文件页缓存属于可回收的整机缓存，不计入网关RSS。

### 整日 snapshots

请求 2026-05-25 整日，共 2,180,128,754 bytes：

| 状态 | 延迟 | 吞吐 |
|---|---:|---:|
| 第一次读取 | 17.21s | 126.7MB/s |
| 页缓存命中，单连接 | 2.74–3.00s | 727–796MB/s |
| 页缓存命中，两连接 | 3.96/4.52s | 聚合963.0MB/s |

第一次读取已经达到标称1Gbps的有效上限。热数据在该虚拟网络中远高于标称值。

### 双流与四流

使用相同的2.18GB页缓存热数据比较：

| 并发流 | 聚合吞吐 | 单请求代表延迟 |
|---:|---:|---:|
| 1 | 729MB/s | 2.99s |
| 2 | 987MB/s | 约4.3s |
| 4 | 1060MB/s | 约8.2s |

四流只比双流增加约7%聚合吞吐，却使单请求延迟接近翻倍；不同冷文件在机械盘上还会增加
寻道。因此生产配置继续使用两个流，并在两个流之上按用户公平轮转。只有一位用户时，
该用户可借用第二条流，调度始终保持 work-conserving。

四个独立用户令牌各连续提交两个完整日请求的真实轮转顺序：

```text
user_d#1, user_c#1
user_b#1, user_a#1
user_c#2, user_d#2
user_a#2, user_b#2
```

首批用户的第二次请求没有越过已经等待的另外两位用户。8个请求共17.44GB，受首轮机械盘
冷读影响总墙钟22.64秒、聚合770MB/s。随后单用户在无人等待时借用两条热流，聚合吞吐
1,147MB/s，说明公平调度没有牺牲空闲时的双流峰值。

### 典型每日窗口

五个交易日每天 09:15–09:25：

```text
objects              10
rows                 2,178,396
Parquet source       38,210,129 bytes
ZSTD Arrow response  34,062,152 bytes
```

仅测 87 → 本机原始5分钟 bundle：

| 指标 | 结果 |
|---|---:|
| p50 | 42.31ms |
| p95 | 43.86ms |
| 单请求吞吐 | 871–930MB/s |

这说明典型请求的网络拉取已经接近“内存挂载盘”行为；最终 Arrow API 的主要成本是本机
Parquet 解码、精确时间过滤和 Arrow 编码。

## 4. 最终本机 FastAPI

统一返回：

```text
application/vnd.apache.arrow.stream
```

同一典型五日窗口、ZSTD Arrow：

| 模式 | 并发 | p50 | p95 | 聚合响应吞吐 |
|---|---:|---:|---:|---:|
| direct | 1 | 1.23s | 1.42s | 28.6MB/s |
| direct | 2 | 1.87s | 2.26s | 36.2MB/s |
| cache 首次填充 | 1 | 1.77s | 1.77s | 19.2MB/s |
| cache 命中 | 1 | 1.17s | 1.37s | 29.5MB/s |
| cache 命中 | 2 | 1.64s | 1.68s | 41.6MB/s |
| cache 命中 | 4 | 2.13s | 2.96s | 63.0MB/s |
| cache 命中 | 8 | 3.11s | 3.18s | 85.4MB/s |
| cache 严格单核 | 1 | 3.18s | 3.24s | 10.9MB/s |

加入动态运行期内存检查后重新测试单日09:15–09:25 snapshots，5次请求的p50为0.220秒、
p95为0.279秒；没有观察到内存监控带来的可见性能下降。

结论：

- direct 远端最多2路；
- cache 不受远端连接槽限制，可独立扩展到本机核心预算；
- 8路总吞吐最高；
- 4路单请求延迟比8路更低；
- 用户可用 `--cores 1` 强制严格单核。

真实五日数据验证：

```json
{
  "direct_rows": 2178396,
  "cache_rows": 2178396,
  "schema_equal": true,
  "content_equal": true
}
```

62列完整响应的时间边界：

```json
{
  "rows": 2178396,
  "columns": 62,
  "min_event_time": "2026-05-25T09:15:00",
  "max_event_time": "2026-05-29T09:24:59.910000"
}
```

## 5. Arrow 编码对照

同一请求、8核、direct：

| 编码 | 响应 bytes | p50 |
|---|---:|---:|
| ZSTD | 34,062,152 | 1.23–1.30s |
| LZ4 | 72,979,272 | 1.61s |
| 无压缩 | 869,247,048 | 3.51s |

ZSTD 同时拥有最小响应和最低端到端延迟，因此作为默认值。

无压缩响应还用于校准本机内存预检：

| dataset | 样本 Arrow/Parquet-source | 正式保守倍率 |
|---|---:|---:|
| snapshots | 22.7x | 24x |
| orders | 13.2x | 16x |
| trades | 6.8x | 10x |

典型窗口最终预检：

```text
estimated_arrow_memory  917,043,096
estimated_working_memory 338,757,008
recommended_workers       8
```

五日全量 snapshots：

```text
source_bytes             11,306,973,618
estimated_arrow_memory  271,367,366,832
estimated_working_memory  3,342,139,712
result                    仅按本机工作内存额度决定
```

`estimated_arrow_memory` 现在只提供给用户了解完整结果规模，不再触发默认2GiB拒绝。API
返回的是有界 Arrow Stream，用户随后逐批消费、保留内存或自行落盘由用户决定。API只在
`estimated_working_memory` 超过本机动态安全额度时提前返回 HTTP 413；运行中低于紧急
内存余量时会主动中止。

单日完整 snapshots 的新预检实测：

```text
source_bytes               2,180,128,754
estimated_arrow_memory    52,323,090,096
estimated_working_memory   3,342,139,712
working_memory_limit      48,455,683,686
result                    accepted
```

尽管完整结果估算超过52GB，API自身的有界流水线工作内存低于本机动态额度，因此不再被
固定结果上限误拒绝。

## 6. 还可以继续优化的部分

当前远端数据面已经比机械盘和常见物理网卡快，继续优化应集中在：

1. 把正式5分钟派生层放在 SSD/NVMe，使不在页缓存中的长区间也能稳定线速；
2. 用 `tools/prewarm.py` 把近期/热门窗口预热到87的Linux页缓存；
3. 若愿意增加约20%–40%派生存储，可额外保存预编码 Arrow IPC fragment，减少本机
   Parquet→Arrow重编码；
4. 低延迟场景把本地核心预算设为4；批量吞吐场景设为8；
5. 原始历史层继续保留不动，所有优化只发生在独立派生层。
